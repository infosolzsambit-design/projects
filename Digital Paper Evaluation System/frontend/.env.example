# Must match backend/.env's API_KEY — sent as X-API-KEY on every request.
VITE_API_KEY=

# Where Vite's dev proxy forwards /api requests (see vite.config.js).
VITE_API_PROXY_TARGET=http://127.0.0.1:8000
